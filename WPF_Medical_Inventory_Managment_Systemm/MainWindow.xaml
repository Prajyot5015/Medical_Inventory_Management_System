<mah:MetroWindow x:Class="WPF_Medical_Inventory_Managment_Systemm.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:WPF_Medical_Inventory_Managment_Systemm"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 mc:Ignorable="d"
                 Title="Medical Inventory Dashboard"
                 Height="700"
                 Width="1200"
                 WindowStartupLocation="CenterScreen"
                 GlowBrush="{DynamicResource AccentTealBrush}"
                 NonActiveGlowBrush="{DynamicResource MediumGrayBrush}"
                 BorderThickness="1">

    <mah:MetroWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- External Styles -->
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Color Definitions -->
            <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#4DB6AC"/>
            <!-- Soft Teal -->
            <SolidColorBrush x:Key="HoverBrush" Color="#81C784"/>
            <!-- Light Mint -->
            <SolidColorBrush x:Key="PressedBrush" Color="#66BB6A"/>
            <!-- Medium Green -->
            <SolidColorBrush x:Key="TextLightBrush" Color="White"/>
            <SolidColorBrush x:Key="TextDarkBrush" Color="#424242"/>
            <!-- Dark Gray -->
            <SolidColorBrush x:Key="MainBackgroundBrush" Color="#FAFAFA"/>
            <!-- Very Light Gray -->
            <SolidColorBrush x:Key="HeaderBrush" Color="#388E3C"/>
            <!-- Darker Green -->

            <!-- Navigation Button Style -->
            <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="15,12"/>
                <Setter Property="Margin" Value="5,2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter Content="{TemplateBinding Content}" 
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" 
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource PressedBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </mah:MetroWindow.Resources>

    <DockPanel>
        <!-- Navigation Panel -->
        <Border DockPanel.Dock="Left" 
                Width="220" 
                Background="{StaticResource SidebarBackgroundBrush}">
            <StackPanel>
                <!-- App Title/Header -->
                <Border Background="{StaticResource HeaderBrush}" Padding="15,20">
                    <TextBlock Text="MEDICAL INVENTORY" 
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- Navigation Buttons -->
                <StackPanel Margin="10,20,10,0">
                    <Button Content="Manufacturers" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="NavigateToManufacturerPage"/>

                    <Button Content="Brands" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="NavigateToBrandPage"/>

                    <Button Content="Products" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="NavigateToProductPage"/>

                    <Button Content="Stock Management" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="NavigateToStockPage"/>

                    <Button Content="Sales" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="NavigateToSalePage"/>
                </StackPanel>

                <!-- Footer/User Area -->
                <Border Background="{StaticResource HeaderBrush}" 
                        Margin="0,20,0,0" 
                        Padding="10"
                        VerticalAlignment="Bottom">
                </Border>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Frame x:Name="MainFrame" 
               NavigationUIVisibility="Hidden"
               Background="{StaticResource MainBackgroundBrush}"/>
    </DockPanel>
</mah:MetroWindow>