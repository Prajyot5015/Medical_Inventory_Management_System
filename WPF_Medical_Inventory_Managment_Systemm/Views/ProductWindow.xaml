<Page x:Class="WPF_Medical_Inventory_Managment_Systemm.Views.ProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Medical_Inventory_Managment_Systemm.Views" xmlns:vm="clr-namespace:WPF_Medical_Inventory_Managment_Systemm.ViewModel"
        mc:Ignorable="d"
        Title="ProductWindow" Height="450" Width="800">

    <Page.DataContext>
        <vm:ProductViewModel/>
    </Page.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Product Entry Form -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBox Width="100" Margin="5" Text="{Binding NewProduct.Name, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="100" Margin="5" Text="{Binding NewProduct.Batch, UpdateSourceTrigger=PropertyChanged}" />
            <DatePicker Width="150" Margin="5" SelectedDate="{Binding NewProduct.ExpiryDate}" />
            <TextBox Width="80" Margin="5" Text="{Binding NewProduct.Unit}" />
            <TextBox Width="80" Margin="5" Text="{Binding NewProduct.Price, UpdateSourceTrigger=PropertyChanged}" />
            <!-- Brand Dropdown -->
            <ComboBox Width="120" Margin="5"
              ItemsSource="{Binding Brands}"
              DisplayMemberPath="Name"
              SelectedValuePath="Id"
              SelectedValue="{Binding NewProduct.BrandId, Mode=TwoWay}" />

            <!-- Manufacturer Dropdown -->
            <ComboBox Width="120" Margin="5"
              ItemsSource="{Binding Manufacturers}"
              DisplayMemberPath="Name"
              SelectedValuePath="Id"
              SelectedValue="{Binding NewProduct.ManufacturerId, Mode=TwoWay}" />

            <Button Content="Add Product" Command="{Binding AddProductCommand}" Margin="5"/>
        </StackPanel>

        <!-- Product List Display -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Batch" Binding="{Binding Batch}" Width="100"/>
                <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryDate}" Width="150"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="80"/>
                <DataGridTextColumn Header="Brand" Binding="{Binding Brand.Name}" Width="100"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer.Name}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
